#Instrucciones API nodepop

Para arrancar normalmente utilizar `$npm start`.

Para arrancar en modo de desarrollo `$npm start dev`.

Para inicializar la BD arrancar con el comando `$npm start installDB`.
